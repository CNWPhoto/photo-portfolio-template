---
const { processSection, theme } = Astro.props;

// TODO:NICHE — fallback step copy references dogs/treats; update for your niche
const heading = processSection?.heading || 'Dog Photography Sessions'; // TODO:NICHE
const tagline  = processSection?.tagline  || 'Simple and relaxed sessions you and your dog will love.'; // TODO:NICHE
const ctaText  = processSection?.ctaText  || 'Learn How it Works';
const ctaLink  = processSection?.ctaLink  || '#';

const steps = [
	{
		label: processSection?.step1Title || 'Reach Out', // TODO:NICHE
		body:  processSection?.step1Body  || "Tell me about your dog — their personality, their quirks, what makes them uniquely them. We'll talk through location options, timing, and everything that will make your session perfect.", // TODO:NICHE
	},
	{
		label: processSection?.step2Title || 'Your Session', // TODO:NICHE
		body:  processSection?.step2Body  || 'Sessions are completely play-based and led by your dog. There are no forced poses, no stiff backdrops — just natural moments, beautiful light, and a whole lot of treats.', // TODO:NICHE
	},
	{
		label: processSection?.step3Title || 'The Images', // TODO:NICHE
		body:  processSection?.step3Body  || 'Within three weeks, your private online gallery arrives. Choose your favorites for fine art prints, heirloom albums, and high-resolution digital downloads.',
	},
];
---

<section class="hstack" id="how-it-works" data-theme={theme || undefined}>
	<hr />
	<div class="hstack__inner">
		<div class="hstack__header reveal">
				<h2 class="hstack__heading">{heading}</h2>
			<p class="hstack__sub">{tagline}</p>
		</div>

		<div class="hstack__timeline">
			{steps.map((step, i) => (
				<div class={`hstack__step reveal reveal-delay-${i + 1}`}>
					<div class="hstack__step-left">
						<div class="hstack__num-wrap">
							<span class="hstack__num">{String(i + 1).padStart(2, '0')}</span>
						</div>
						{i < steps.length - 1 && <div class="hstack__connector" />}
					</div>
					<div class="hstack__step-right">
						<h3 class="hstack__step-label">{step.label}</h3>
						<p class="hstack__step-body">{step.body}</p>
					</div>
				</div>
			))}
		</div>

		<div class="hstack__footer reveal reveal-delay-4">
			<a href={ctaLink} class="hstack__link">{ctaText} &rarr;</a>
		</div>
	</div>
</section>

<style>
	.hstack { padding-top: 0; padding-bottom: 7rem; }

	.hstack__inner {
		max-width: 860px;
		margin: 0 auto;
		padding: 0 3rem;
	}

	.hstack__header {
		padding: 5.5rem 0 5rem;
		text-align: center;
	}

	.hstack__heading {
		font-size: clamp(2rem, 4vw, 3.25rem);
		color: var(--text);
		margin-bottom: 1.25rem;
		letter-spacing: -0.01em;
	}

	.hstack__sub {
		font-size: 1.275rem;
		color: var(--muted);
		font-style: italic;
		line-height: 1.6;
	}

	.hstack__sub em {
		font-style: normal;
		text-transform: uppercase;
		letter-spacing: 0.06em;
		font-size: 0.95em;
		color: var(--accent);
	}

	/* ── Timeline ── */
	.hstack__timeline {
		display: flex;
		flex-direction: column;
	}

	.hstack__step {
		display: grid;
		grid-template-columns: 56px 1fr;
		gap: 0 2.5rem;
	}

	/* Left column: number circle + connector line */
	.hstack__step-left {
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.hstack__num-wrap {
		width: 56px;
		height: 56px;
		border: 1px solid var(--border);
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		background: var(--bg);
	}

	.hstack__num {
		font-family: var(--font-sans);
		font-size: 0.84rem;
		letter-spacing: 0.16em;
		color: var(--accent);
	}

	.hstack__connector {
		width: 1px;
		flex: 1;
		min-height: 3rem;
		background: var(--border);
		margin: 0.5rem 0;
	}

	/* Right column: label + body */
	.hstack__step-right {
		padding-bottom: 3.5rem;
		padding-top: 0.875rem; /* vertically align with number center */
	}

	.hstack__step:last-child .hstack__step-right {
		padding-bottom: 0;
	}

	.hstack__step-label {
		font-family: var(--font-sans);
		font-style: normal;
		font-size: 0.84rem;
		letter-spacing: 0.2em;
		text-transform: uppercase;
		color: var(--text);
		margin-bottom: 1rem;
		font-weight: 600;
	}

	.hstack__step-body {
		font-style: normal;
		font-size: 1.125rem;
		line-height: 1.78;
		color: var(--muted);
	}

	/* Footer */
	.hstack__footer {
		margin-top: 3rem;
		padding-top: 2rem;
		border-top: 1px solid var(--border);
		text-align: right;
	}

	.hstack__link {
		font-style: italic;
		font-size: 1.2rem;
		color: var(--accent);
		text-decoration: none;
		letter-spacing: 0.01em;
	}

	@media (max-width: 768px) {
		.hstack__inner { padding: 0 1.5rem; }
		.hstack__step { grid-template-columns: 44px 1fr; gap: 0 1.5rem; }
		.hstack__num-wrap { width: 44px; height: 44px; }
		.hstack__footer { text-align: center; }
	}
</style>
