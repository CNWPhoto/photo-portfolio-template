---
const { faqs: sanityFaqs, theme = 'classic-cream' } = Astro.props;

// TODO:NICHE — fallback FAQs reference dogs/breeds/treats; update for your niche
const fallbackFaqs = [
	{ q: 'What should I bring to the session?', a: "Your dog's favorite treats and a toy or two make a real difference in keeping them engaged and happy. Comfortable shoes are a must for you — we'll be moving around together to find the best light and moments." }, // TODO:NICHE
	{ q: 'How long does a typical session last?', a: "Most sessions run about 90 minutes. This gives us plenty of time to let your dog settle in, explore the location, and capture a wonderful range of images — all without feeling rushed." },
	{ q: 'When will I receive my photos?', a: 'Your private online gallery will be ready within three weeks of your session date. Rush delivery within one week is available on request for an additional fee.' },
	{ q: 'Do you photograph dogs of all breeds and temperaments?', a: "Absolutely. From shy rescues to exuberant puppies, every dog deserves beautiful portraits. Sessions are entirely play-based and follow your dog's lead — there is never any pressure." }, // TODO:NICHE
];

const faqs =
	sanityFaqs?.length
		? sanityFaqs.map((f: any) => ({ q: f.question, a: f.answer }))
		: fallbackFaqs;
---

<section class="faqs" id="faqs" data-theme={theme}>
	<hr />
	<div class="faqs__inner">
		<div class="faqs__label-col" aria-hidden="true">
			<span class="faqs__label">FAQs</span>
		</div>

		<div class="faqs__content">
			<div class="faqs__list">
				{
					faqs.map((faq) => (
						<div class="faqs__item">
							<h3 class="faqs__question">{faq.q}</h3>
							<blockquote class="faqs__answer">{faq.a}</blockquote>
						</div>
					))
				}
			</div>

			<div class="faqs__footer">
				<a href="#" class="faqs__link">View all FAQs &rarr;</a>
			</div>
		</div>
	</div>
</section>

<style>
	.faqs {
		padding-bottom: 7rem;
	}

	.faqs__inner {
		display: grid;
		grid-template-columns: 100px 1fr;
		padding: 5rem 3rem 0;
		max-width: 1200px;
		margin: 0 auto;
		gap: 2rem;
	}

	/* Rotated label */
	.faqs__label-col {
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.faqs__label {
		font-family: var(--font-serif);
		font-style: italic;
		font-size: clamp(2rem, 3.5vw, 3.25rem);
		letter-spacing: 0.1em;
		color: var(--border);
		writing-mode: vertical-rl;
		transform: rotate(180deg);
		white-space: nowrap;
		user-select: none;
	}

	/* FAQ list */
	.faqs__list {
		display: flex;
		flex-direction: column;
		gap: 0;
	}

	.faqs__item {
		padding: 2.5rem 0;
		border-bottom: 1px solid var(--border);
	}

	.faqs__item:first-child {
		border-top: 1px solid var(--border);
	}

	.faqs__question {
		font-size: 1.25rem;
		color: var(--text);
		margin-bottom: 1.25rem;
		letter-spacing: -0.01em;
	}

	.faqs__answer {
		margin: 0;
		padding: 0 0 0 1.75rem;
		border-left: 2px solid var(--accent);
		font-style: normal;
		font-size: 0.9375rem;
		line-height: 1.78;
		color: var(--muted);
	}

	/* Footer link */
	.faqs__footer {
		padding-top: 2.5rem;
	}

	.faqs__link {
		font-style: italic;
		font-size: 1rem;
		color: var(--accent);
		text-decoration: none;
		transition: opacity 0.2s;
	}

	.faqs__link:hover {
		opacity: 0.7;
	}

	@media (max-width: 768px) {
		.faqs__inner {
			grid-template-columns: 1fr;
			padding: 4rem 1.5rem 0;
		}

		.faqs__label-col {
			display: none;
		}
	}
</style>
