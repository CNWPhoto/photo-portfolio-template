# ── Blackbird Photography (Melanie Fogel) ──
# Restore via: cp studio/.env.blackbird-photography-backup studio/.env
# Then: cd studio && npm run deploy (if you need to redeploy her Studio)

SANITY_STUDIO_PROJECT_ID=6nc24jar
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_HOST=blackbird-photography
SANITY_STUDIO_TITLE=Black Bird Photography

# Canonical preview URL — currently the temporary Cloudflare Pages domain.
# Update to her custom domain once Phase 3 (DNS) is complete.
SANITY_STUDIO_PREVIEW_URL=https://blackbird-photography.pages.dev

# Pinned after first deploy. Left blank initially — Sanity will create a
# new application and print the appId in the CLI output; paste it here
# so subsequent deploys are non-interactive.
SANITY_STUDIO_APP_ID=re7vy9kvaexioskf1e0jsu8a

# AI Assist plugin on by default during the trial month.
# Remove this line and redeploy Studio if Melanie opts out after the trial.
SANITY_STUDIO_AI_ASSIST=true
