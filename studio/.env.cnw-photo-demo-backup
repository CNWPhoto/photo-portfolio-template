# Origin of the Astro site that Studio Presentation will load in its iframe.
# Local dev: http://localhost:4321
# Deployed:  https://<client-domain> (production) or a preview URL
SANITY_STUDIO_PREVIEW_URL=https://cnw-photo-demo.connor-213.workers.dev

# Preserves the demo Studio's pinned appId so `npm run deploy` stays
# non-interactive when restoring this file as the active .env.
SANITY_STUDIO_APP_ID=in1qs4npb7ua5ico8wxd7lge

# AI Assist plugin on for the demo so it can be shown to prospects
# when screen-sharing Studio. Bake-time gate.
SANITY_STUDIO_AI_ASSIST=true
