# ── Lavon Photography ──
# Restore via: cp studio/.env.lavon-photography-backup studio/.env
# Then: cd studio && npm run deploy (if you need to redeploy her Studio)

SANITY_STUDIO_PROJECT_ID=3a8494gh
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_HOST=lavon-photography
SANITY_STUDIO_TITLE=Lavon Photography

# Canonical preview URL — currently the temporary Cloudflare Pages domain.
# Update to her custom domain once Phase 3 (DNS) is complete.
SANITY_STUDIO_PREVIEW_URL=https://www.lavonphotography.com

# Pinned after first deploy so subsequent `npm run deploy` calls skip the
# "create new application" prompt.
SANITY_STUDIO_APP_ID=qk3hzrby82vk3b9jhizx58vy

# AI Assist plugin on by default during the trial month.
# Remove this line and redeploy Studio if Kris opts out after the trial.
SANITY_STUDIO_AI_ASSIST=true
