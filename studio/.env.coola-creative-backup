# ── Coola Creative (Carla Jacobson) ──
# Restore via: cp studio/.env.coola-creative-backup studio/.env
# Then: cd studio && npm run deploy (if you need to redeploy her Studio)

SANITY_STUDIO_PROJECT_ID=tl3zj8iz
SANITY_STUDIO_DATASET=production
SANITY_STUDIO_HOST=coola-creative
SANITY_STUDIO_TITLE=Coola Creative

# Canonical production URL (now served by the Astro 6 Cloudflare Worker
# after the 2026-05-19 Pages→Workers domain cutover).
SANITY_STUDIO_PREVIEW_URL=https://coolacreative.com

# Pinned after first deploy so subsequent `npm run deploy` calls skip the
# "create new application" flow.
SANITY_STUDIO_APP_ID=a2zsj4jpbc1xb0kijfsdq595

# AI Assist plugin on by default during the trial month.
# Remove this line and redeploy Studio if Carla opts out after the trial.
SANITY_STUDIO_AI_ASSIST=true
